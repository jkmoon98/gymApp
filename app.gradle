apply plugin: "com.android.application"

android {
    namespace "com.gym.app"
    compileSdk 34

    defaultConfig {
        applicationId "com.gym.app"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        debug {
            debuggable true
        }
    }
}

dependencies {
}
